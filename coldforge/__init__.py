"""coldforge — honest, local-first cold outreach toolkit.

Research → personalize → sequence → send → follow, with a shared core powering
both a CLI and an MCP server. No SaaS required; degrades gracefully when optional
API keys / dependencies are absent.
"""

__version__ = "0.1.0"
